# RentEase - Supabase, GitHub & Vercel Integration Plan

## Goal
Connect the RentEase app to Supabase (backend), GitHub (code storage), and Vercel (deployment).

---

## Inputs Provided
- **Supabase URL**: `https://knioiuxmfxaeawcsrtlr.supabase.co`
- **Supabase anon key**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
- **GitHub username**: `Hilltop256`
- **Vercel email**: `hilltopnaguru@gmail.com`

---

## Step 1: Connect Supabase
1. Create `.env` file in `app/` directory with:
   ```
   VITE_SUPABASE_URL=https://knioiuxmfxaeawcsrtlr.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImtuaW9pdXhtZnhhZWF3Y3NydGxyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzU1MTA0NDgsImV4cCI6MjA5MTA4NjQ0OH0.l1AmqskLt9qNFgj8Al9qaYk2UyVlU-q2Ntv2-fTmxVA
   ```
2. Update `app/src/lib/utils.ts` to add Supabase client initialization
3. Create Supabase client file: `app/src/lib/supabase.ts`
4. Update `app/src/contexts/AuthContext.tsx` to use Supabase auth instead of mock data

---

## Step 2: Create GitHub Repository
1. Navigate to `app/` directory
2. Initialize git: `git init`
3. Create initial commit with all source code
4. Create GitHub repo named `rentease` under user `Hilltop256`
5. Push code to GitHub

---

## Step 3: Connect Vercel for Deployment
1. Use Vercel CLI or manual import:
   - Go to vercel.com → Add New → Project
   - Import from GitHub repo `Hilltop256/rentease`
   - Add environment variables:
     - `VITE_SUPABASE_URL`
     - `VITE_SUPABASE_ANON_KEY`
   - Deploy

---

## Step 4: Verify All Connections
1. Test Supabase auth flow in the app
2. Confirm GitHub repo has all code
3. Verify Vercel deployment works

---

## File Locations After Setup
- **App code**: `app/` directory → GitHub repo `Hilltop256/rentease`
- **Deployed**: Vercel project linked to GitHub
- **Backend**: Supabase project `knioiuxmfxaeawcsrtlr`

---

## Notes
- Any code changes pushed to GitHub will automatically deploy to Vercel
- Supabase handles authentication and database